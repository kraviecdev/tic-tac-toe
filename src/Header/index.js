import { StyledHeader, StyledTitle } from "./styled"

const Header = () => {

    return (
        <StyledHeader>
            <StyledTitle>
                {"Tic Tac Toe"}
            </StyledTitle>
        </StyledHeader>
    );
}

export default Header;